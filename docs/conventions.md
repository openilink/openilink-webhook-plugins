# Conventions

- One plugin per directory under `plugins/`
- Main plugin file should be named `plugin.js`
- Add a local README for each plugin
- Keep examples and screenshots inside the plugin directory
